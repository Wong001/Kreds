<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application>
        <service
            android:name="expo.modules.tormanager.TorNodeService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Task 3 (B.2d): AVIF decode sandbox. isolatedProcess gives it an
             ephemeral, permissionless UID with no app-data access and no
             network, so a decoder RCE on friend-authored (un-re-gated) image
             bytes is contained here, away from the node's keys/store/socket.
             All data crosses via IPC (ParcelFileDescriptor pipes). -->
        <service
            android:name="expo.modules.tormanager.ImageDecodeService"
            android:isolatedProcess="true"
            android:process=":imagedecode"
            android:exported="false" />
    </application>
</manifest>
